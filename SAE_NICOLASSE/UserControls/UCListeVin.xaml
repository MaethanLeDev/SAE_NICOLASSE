<UserControl x:Class="SAE_NICOLASSE.UserControls.UCListeVin"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SAE_NICOLASSE.UserControls"
             xmlns:model="clr-namespace:SAE_NICOLASSE.Classe"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- Ligne du haut : Barre de recherche -->
            <RowDefinition Height="*" />
            <!-- Ligne du bas : Catalogue des vins -->
        </Grid.RowDefinitions>

        <!-- LIGNE DU HAUT : Barre de recherche et filtres -->
        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Barre de recherche -->
            <Border Grid.Column="0"
                    CornerRadius="6"
                    BorderBrush="#8B1538"
                    BorderThickness="1"
                    Background="White"
                    Margin="0,0,10,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Icône de recherche -->
                    <TextBlock Grid.Column="0"
                               Text="🔍"
                               FontSize="16"
                               Foreground="#8B1538"
                               VerticalAlignment="Center"
                               Margin="10,0,5,0" />

                    <!-- TextBox de recherche -->
                    <TextBox x:Name="txtRecherche"
                             Grid.Column="1"
                             FontSize="14"
                             Padding="5,8"
                             BorderThickness="0"
                             Background="Transparent"
                             VerticalAlignment="Center"
                             Foreground="#333333">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TextBox">
                                            <Grid>
                                                <ScrollViewer x:Name="PART_ContentHost" />
                                                <TextBlock Text="Rechercher un vin..."
                                                           Foreground="#AAAAAA"
                                                           VerticalAlignment="Center"
                                                           Margin="5,0,0,0"
                                                           IsHitTestVisible="False">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </Grid>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </Grid>
            </Border>

            <!-- Bouton Filtre -->
            <Button x:Name="btnFiltre"
                    Grid.Column="1"
                    Content="Filtres"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Padding="20,8"
                    Background="#8B1538"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="6"
                                            BorderThickness="0">
                                        <ContentPresenter HorizontalAlignment="Center"
                                                          VerticalAlignment="Center" />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#A01A42" />
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter Property="Background" Value="#7A1235" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>

        <!-- LIGNE DU BAS : Catalogue des vins -->
        <Border Grid.Row="1"
                CornerRadius="8"
                BorderBrush="#8B1538"
                BorderThickness="2"
                Background="White">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          Margin="10">
                <ItemsControl x:Name="itemControlVins">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type model:Vin}">
                            <local:UCAffichageVin />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>